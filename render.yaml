services:
  - type: web
    name: devendra-astrochatbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    pythonVersion: 3.13.4
